import React, { Component } from 'react'

const PPS = (props) => {
    const {selectedProtocol, loading} = props

    return (
        <div className="pps-flex">
            <div className="pps-flex-left">
                

            </div>

        </div>
    )
}